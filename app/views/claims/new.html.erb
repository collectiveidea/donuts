<% if @users_claiming_today.any? %>
  <%= @users_claiming_today.map { |u| link_to(u.display_name, u.github_url) }.to_sentence.html_safe %>
  <%= @users_claiming_today.one? ? "is" : "are" %> already briging in donuts today!
<% end %>

<%= button_to "Donuts!", claim_path, method: :get, disabled: current_user.try(:claimed_today?), class: "claim-button" %>
